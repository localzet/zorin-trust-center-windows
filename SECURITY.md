# Security

Zorin Trust Center is a local visualization/control surface. It does not replace host-agent policy checks and never treats UI state as an authorization primitive. Sensitive actions still go through the authenticated host control path and phone-signed Owner Proof flow.
