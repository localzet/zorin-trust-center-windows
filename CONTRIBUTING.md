# Contributing

Keep the Trust Center read-mostly. Any button that changes trust or credentials must delegate to the host agent or an existing explicit helper flow. Do not parse a green UI indicator as authorization.
